server:
  port: 8080

spring:
  application:
    name: market-data-persister

  rabbitmq:
    host: localhost
    port: 5672
    username: guest
    password: guest

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/rabbittick?useUnicode=true&characterEncoding=UTF-8&rewriteBatchedStatements=true
    username: root
    password: password
    hikari:
      maximum-pool-size: 20

  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          batch_size: 50
          order_inserts: true

app:
  rabbitmq:
    exchange: market-data.exchange
    routing-key-ticker: "*.ticker.#"
    routing-key-trade: "*.trade.#"
    routing-key-orderbook: "*.orderbook.#"
    dlq-exchange: market-data.dlx
    dlq-queue: market-data.persist.dlq
    dlq-routing-key: market-data.persist.dlq
    retry-max-attempts: 3
    prefetch-count: 100

    ticker-concurrent-consumers: 2
    ticker-max-concurrent-consumers: 4

    trade-concurrent-consumers: 2
    trade-max-concurrent-consumers: 4

    orderbook-concurrent-consumers: 6
    orderbook-max-concurrent-consumers: 12

management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  endpoint:
    prometheus:
      enabled: true
  metrics:
    tags:
      application: ${spring.application.name}